.Left
  = form_for(@note) do |f|
    - if @note.errors.any?
      .error_explanation
      %h2 
        = pluralize(@note.errors.count, "erro")
      %ul
        - @note.errors.full_messages.each do |msg|
          %li 
            = msg

    .field
      = f.label :text
      %br
      = f.text_area :text
    .field
      = label_tag :tags
      %br
      = text_field_tag :tags, @note.tag_list.join(', ')
    .actions
      = f.submit
    = link_to 'Voltar', notes_path
.Right
  #help_text
  %h2 Formatando o texto
  %p
    A formatação é feita com o 
    %em 
      = link_to 'Textile', "http://redcloth.org/hobix.com/textile/"
    \.
  %p
    Possibilitando uma formatação simples, atráves de marcações fáceis.

  %p
    Segue alguns exemplos:

  .textileexamples
    %p
      *negrito* → <strong>negrito</strong>
      %br
      _itálico_ → 
      %em 
        itálico
      %br
      "link":http://google.com → 
      = link_to "link","http://google.com"
      %br
      pre. →  texto pré-formatado (<i>pre</i> do html)
      %br
      * → lista com marcador
      %br
      ** → segundo nível
      %br
      \# → lista numerada
      %br
      \## → segundo nível
    
