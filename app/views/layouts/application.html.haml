!!!
%html(lang='pt-BR')
  %head
    %meta(charset='utf-8')
    %title Snote - notes made easy
  = stylesheet_link_tag :all
  = javascript_include_tag :defaults
  = csrf_meta_tag

  %body
    #snote
      = image_tag "/images/snote_title.png", :id =>'logo'
      = link_to "Logout", destroy_user_session_path, :id =>'logout_link' if current_user
      = yield
    = google_analytics

